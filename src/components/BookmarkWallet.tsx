
import React, { useRef, useCallback } from 'react';
import { useState, useEffect } from 'react';
import Typography from '@mui/material/Typography';
import Button from '@mui/material/Button';
import Input from '@mui/material/Input';
import axios from 'axios';
import './DataRender.css';

export const DataRender: React.FC = () => {
const [bookMarkedWallet, setBookMarkedWallet] = useState('');
const inputRef = React.useRef<HTMLInputElement>(null);
   

// map bookmarked wallets into side column container
// 

    return (<div>bookmarked wallets:</div>
    bookmarkedWallets.map((item) => {
        item.
    })
    
    
    
    );

};