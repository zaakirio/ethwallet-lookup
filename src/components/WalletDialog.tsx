
import * as React from "react";
import "./WalletDialog.css";

export default function WalletDialog() {

  return (
    //   Once a user clicks on the add wallet button, takes them to this interface.
    //   This interface will have an editable table with a name wallet address and amount 
    //   with a refresh button in each entry to run an API call to get latest balance
      <></>

  );
}
